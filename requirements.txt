# Core HTTP + parsing
requests==2.32.3            # Robust HTTP client
beautifulsoup4==4.12.3      # HTML parsing
lxml==5.2.2                 # Fast parser for BeautifulSoup
tqdm==4.66.4                # Progress bars

# Web Framework
Flask==3.0.0                # Web application framework
Werkzeug==3.0.1             # WSGI utilities
gunicorn>=21.2.0,<22.0.0    # Production WSGI server

# Data handling / utilities
pandas>=2.2.2               # Optional tabular transforms (flexible version)
python-dateutil==2.9.0.post0
numpy>=1.24.0               # Required for advanced embeddings

# Embeddings + vector store (upgraded for better accuracy)
sentence-transformers>=2.7.0,<3.0.0  # Latest embedding models with better accuracy
chromadb>=1.2.0,<2.0.0      # Updated ChromaDB with performance improvements
torch>=2.0.0                # PyTorch for better embedding performance
transformers>=4.30.0        # Latest transformers for model support

# Enhanced RAG capabilities
scikit-learn>=1.3.0         # For advanced similarity calculations
faiss-cpu>=1.7.4           # Optional: Fast similarity search
rank-bm25>=0.2.2           # Hybrid search capabilities

# LangChain toolkit (updated to latest compatible versions)
langchain>=0.2.11,<0.4.0
langchain-community>=0.2.10,<0.4.0  
langchain-text-splitters>=0.2.2,<0.4.0

# API
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# Settings/validation
pydantic>=2.8.2,<3.0.0
pydantic-settings>=2.3.4,<3.0.0

# Async/HTTP client (optional helpers)
httpx>=0.27.0,<1.0.0

# Testing
pytest>=8.2.2,<9.0.0

# Env loading
python-dotenv>=1.0.1,<2.0.0

# Reporting / documentation generation
python-docx==1.1.2

